TWITCH_CLIENT_ID=your_client_id
TWITCH_CLIENT_SECRET=your_client_secret
JWT_SECRET=your_random_secret_here
GEMINI_API_KEY=
INTERNAL_API_SECRET=filadbd123

# Optional — "you're live" Web Push. Without these the feature stays off.
# Generate the pair with: bun scripts/generate-vapid-keys.ts
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
# Signs Twitch EventSub webhooks; any random string: openssl rand -hex 32
EVENTSUB_SECRET=
